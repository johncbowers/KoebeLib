package geometry.primitives.Spherical2

/**
 * Created by johnbowers on 5/22/17.
 *
 * Stored as Plücker coordinates of the dual line in the circle-plane duality.
 *
 * Choice of coefficient ordering taken from Stolfi's PhD thesis.
 */

// TODO

class CoaxialFamily(
        val a: Double,
        val b: Double,
        val c: Double,
        val d: Double,
        val e: Double,
        val f: Double
) {

    //constructor (source: DiskS2, target: DiskS2)
    //constructor (c: CoaxialFamily) : this(DiskS2(c.source), DiskS2(c.target))


}